#pragma once

#include <iostream>
#include <math.h>

typedef enum STATE {
    MENU,
    MATRIX,
    FRACTIONS,
    COMBINATORICS,
    FUNCTIONS,
    STATISTICS,
    POLYNOMIALS,
    EXIT
} STATE;

using namespace std;